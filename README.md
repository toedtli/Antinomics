# Installation of the virtualenv
    
    cd Antinomics/ #project root
    python3 -m venv venv
    . ./venv/bin/activate
    pip install -r requirements.txt
    python -m ipykernel install --user --name tide

then start jupyter lab

    jupyter lab .


